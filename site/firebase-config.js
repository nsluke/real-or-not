// Firebase web config. These values are public by design — access is controlled by
// the Firestore security rules in ../firestore.rules, not by hiding this file.
// Fill these in from Firebase console → Project settings → Your apps → Web app.
export default {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  appId: 'YOUR_APP_ID',
};
